const Header = () => {
    // const headerCSS = {
    //     // use camelCasing style for the CSS style properties; do not use a "-"
    //     fontSize: "48px"
    // };
    // style={headerCSS} ->>this will go in the element you want to style

    return (
        <header>
            <h1>Hello World</h1>
        </header>
    );
}

export default Header;